name: "dashboard"
services:
  frontend:
    container_name: frontend
    build: ./dashboard-ui
    ports:
      - "3000:3000"

  backend:
    container_name: backend
    build: ./dashboardAPI
    env_file: .env
    ports:
      - "8000:8000"
